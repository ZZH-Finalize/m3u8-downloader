{
  "name": "m3u8-downloader-extension",
  "version": "1.0.0",
  "description": "m3u8 视频下载器管理插件",
  "private": true,
  "scripts": {
    "generate": "node generate-icons.js",
    "build": "npm run generate && node build.js",
    "build:skip-icons": "node build.js"
  },
  "dependencies": {
    "archiver": "^7.0.0",
    "sharp": "^0.33.0"
  }
}
